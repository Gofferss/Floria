-- ================================================================
-- 012 — Отметка «принят в обработку флористом».
--
-- Отдельно от orders.status: статус — это стадия исполнения (новый →
-- подтверждён → собирается → ...), а этот флаг — просто "флорист
-- увидел и взял в работу", быстрый чек-бокс в списке заказов, без
-- необходимости открывать карточку и выбирать стадию.
-- ================================================================

begin;

alter table orders add column if not exists florist_accepted_at timestamptz;

commit;
