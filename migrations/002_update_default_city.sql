-- ⚠️ ПОГЛОЩЕНО migrations/001_setup_full_db.sql — для новой/пустой базы
-- запускайте только 001, этот файл не нужен. Оставлен для истории и для
-- тех, кто уже применил старую floria_schema.sql отдельно и хочет
-- накатить только смену дефолтного города.
-- ================================================================
-- Миграция 002: смена дефолтного города с 'Москва' на 'Симферополь'
-- Применять после floria_schema.sql на уже развёрнутой базе.
-- ================================================================

-- 1. Меняем DEFAULT для новых строк
alter table customer_addresses
  alter column city set default 'Симферополь';

alter table orders
  alter column delivery_city set default 'Симферополь';

-- 2. (Опционально) Обновить уже существующие строки, где стоит старый
-- дефолт. Раскомментируйте, только если уверены, что эти строки —
-- действительно тестовые/дефолтные записи, а не реальные заказы с
-- осознанно указанной Москвой (например, если планируется доставка
-- в другие города в будущем — тогда лучше НЕ трогать исторические данные).

-- update customer_addresses set city = 'Симферополь' where city = 'Москва';
-- update orders set delivery_city = 'Симферополь' where delivery_city = 'Москва';
